import { PwdValidatorsDirective } from './pwd-validators.directive';

describe('PwdValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
